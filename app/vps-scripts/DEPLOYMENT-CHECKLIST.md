* Subdomain root never reuses the static source of any other deployment
* Nginx config generated with onboard-subdomain.sh and template
* Cloudflare DNS and cache verified with each deploy
* Playwright/QA scripts run and pass
* .env.production and secrets.env match ONLY this subdomain
* Logs and audit files isolated and checked
